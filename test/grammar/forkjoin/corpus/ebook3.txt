# ebook3
VAR_D=2;
VAR_E=2;

A;
FORK ROT_C;
B;
FORK ROT_E';
JOIN VAR_D, ROT_D, QUIT;

ROT_C: C;
  JOIN VAR_D, ROT_D, QUIT;

ROT_D: D;
  JOIN VAR_E , ROT_E , QUIT ;

ROT_E': JOIN VAR_E, ROT_E, QUIT;

ROT_E: E;
  QUIT;
==>
Program(Assign(Label,Number),Assign(Label,Number),Call(Label),Fork(Label),Call(Label),Fork(Label),Join(Label,Label,Label),Def(Label),Call(Label),Join(Label,Label,Label),Def(Label),Call(Label),Join(Label,Label,Label),Def(Label),Join(Label,Label,Label),Def(Label),Call(Label),Call(Label))
