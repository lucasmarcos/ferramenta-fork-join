import assert from "node:assert";
import { test } from "node:test";

import { treewalk } from "../out/treewalk.js";

test("treewalk", (t) => {
  /*
  const input = {};
  const output = treewalk(input);
  const expected = {};
  assert.deepEqual(output, expected);
  */
});
