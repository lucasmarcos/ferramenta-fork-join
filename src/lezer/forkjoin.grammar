@top Program { command* }

command { Fork | Join | Assign | Call | Def }

Fork { @specialize<Label, "FORK"> Label ";" }

Join { @specialize<Label, "JOIN"> Label "," Label "," Label ";" }

Assign { Label "=" Number ";" }

Call { Label ";" }

Def { Label ":" }

@skip { space | comment }

@tokens {
  Label { (@asciiLetter | "_") (@asciiLetter | @digit | "_" | "'")* }
  Number { @digit+ }
  space { @whitespace+ }
  comment { "//" ![\n]* }
}
